import React from 'react';
import { Box, Typography, Link, Grid, Container, Toolbar, Button } from '@mui/material';
import Image from 'next/image';

const Footer: React.FC = () => {
    return (
       <></>
    );
};

export default Footer;
